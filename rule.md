# 앱 접근성 핵심 규칙 3종

이 문서는 Figma Accessibility Checker 플러그인이 검사하는 핵심 접근성 규칙을 정의합니다.

---

## 1. 색에 무관한 콘텐츠 인식 (Color-Independent Information)

### 규칙 (Rule)

화면에 표시되는 모든 정보는 색에 의존하지 않고도 인식 가능해야 한다. 색만으로 상태·의미·선택 여부를 전달해서는 안 된다.

### 문제 패턴 (Common Failure Patterns)

- **동일 형태 색상 변화**: 동일한 형태(모양·아이콘·컴포넌트)인데 색만 변화
- **상태 구분 부족**: 선택/미선택 상태가 색 변화만으로 구분됨
- **상태 표시 부족**: 에러/성공 상태가 색(빨강/초록)만으로 표시됨
- **동일 형태 상태 그룹**: 탭·날짜 선택 등 동일한 버튼/프레임이 색(채우기/테두리)만 달라짐
- **차트 색상 의존**: 데이터 차트에서 색만 구분 요소로 사용

### 해결책 (Fix Guidelines)

- **형태 기반 구분**: 디자인 상태 변화는 형태 기반 요소(패턴, 테두리, 굵기, 모양)로도 구분하게 할 것
- **텍스트 강조**: 텍스트 상태 구분 시 폰트 굵기(bold) 등 시각적 차이 추가
- **차트 개선**: 차트/그래프는 패턴·아이콘·레이블 추가
- **병행 제공**: 필수 정보는 색 외에도 문자·도형·언어로 병행 제공

### 검사 기준

- 3개 이상의 동일한 형태 도형이 색상만 다르게 배치된 경우 감지
- 2개 이상 동일 크기·형태(탭/버튼/프레임) 상태 그룹이 색상 또는 테두리 색만 달라지는 경우 감지
- 인디케이터, 버튼, 상태 표시 등에서 색상만으로 구분되는 패턴 탐지
- 텍스트 굵기·테두리 두께·패턴 등 색 이외 구분 수단이 있는 경우는 예외로 처리

---

## 2. 텍스트/이미지 콘텐츠의 명도 대비 (Contrast Ratio)

### 규칙 (Rule)

색약/저시력 사용자도 콘텐츠를 인식할 수 있도록 텍스트와 UI 요소는 WCAG 기준 이상의 명도 대비를 가져야 한다.

### 대비 기준

- **텍스트**: 전경/배경 대비 **3:1 이상**
- **UI 요소/아이콘**: 전경/배경 대비 **3:1 이상** (WCAG AA 기준)
- **큰 텍스트**: 18pt 이상 또는 14pt 이상 굵은 글씨는 **3:1 이상**
- **요약**: 모든 텍스트(큰 텍스트 포함) 3:1 이상, UI 컴포넌트/아이콘 3:1 이상

### 문제 패턴 (Common Failure Patterns)

- **디자인 요소 대비 부족**: 아이콘·버튼·라인 등 전경/배경 대비 < 3:1
- **텍스트 대비 부족**: 텍스트(라벨·메뉴·본문)와 배경 대비 < 3:1
- **배경 이미지 위 텍스트**: 배경 이미지 위 텍스트가 충분한 대비 확보 실패
- **상태 구분 어려움**: disabled 상태와 normal 상태 색이 구분되지 않음

### 해결책 (Fix Guidelines)

- **대비 조정**: 전경색/배경색 대비가 기준치 이상 되도록 조정
  - 텍스트: 3:1 이상
  - UI 요소/아이콘: 3:1 이상
- **배경 이미지 처리**: 배경 이미지 위 텍스트에는 overlay(40~60%) 적용
- **상태 구분**: disabled 상태는 대비를 낮추되 의미 전달 요소는 제외
- **보조 수단**: 대비를 높일 수 없는 경우 텍스트 외곽선, 그림자 등 추가

### 검사 기준

- 텍스트 노드: 3:1 기준 (기본값, 최소 3:1 이상 설정 가능)
- 도형/아이콘 노드: 3:1 기준 (기본값, 최소 2:1 이상 설정 가능)
  - 전경/배경이 동일 색상으로 계산되어 1:1(또는 3:1 미만)인 동일 요소는 대비 검사에서 제외
  - 이름/부모에 UI 키워드(btn/icon/tab 등)가 없고 주변 텍스트도 없는 장식용 도형/아이콘은 대비 검사 제외
- 부모/조상 또는 같은 그룹·조상 그룹의 덮는(covering) 배경을 찾아 계산하며, 배경을 찾지 못하면 대비 계산을 건너뛰고 안내 메시지를 표시
- 같은 계층(sibling)의 배경도 색 계산 대상으로 삼지만, 텍스트 뒤에 있고 텍스트 영역을 충분히 덮어야 한다(최소 20% 이상 겹침, 60% 이상이면 확실). 텍스트의 중심점이 배경 사각형 안에 들어가거나, 텍스트보다 큰 배경이 10% 이상 겹치면 배경으로 인정한다. 배경 레이어는 텍스트보다 뒤로 두고 투명도 10% 이상, SOLID/GRADIENT Fill로 설정할 것
- **참고**: 1:1 대비는 의미가 없으므로 최소값 제한이 적용됩니다

### 제외 규칙 (Non-text / Icon)

- **장식/히트 배경 제외**: `bg`, `background`, `container`, `hit area` 등으로 명명된 장식용/히트 영역 배경은 대비 검사에서 제외
- **저채도/저투명 배경 제외**: fill opacity ≤ 0.25 또는 명도 차이가 1.2:1 이하인 도형은 대비 검사에서 제외
- **백플레이트 제외**: 동일 그룹에서 가장 큰 단일 도형이며 내부에 아이콘/텍스트가 있는 경우(백플레이트 역할) 대비 검사에서 제외
- **장식 아이콘/이미지 제외**: 버튼·탭 등 UI 키워드/반응이 없고 주변(≈180px)에 텍스트가 없으며 작은 단일 아이콘/벡터(짧은 변 ≤ 48px 등) 또는 `illust/image/decor` 명명 일러스트는 대비 검사에서 제외

---

## 3. 자동으로 변경되는 콘텐츠 (Auto-Updating Content / Carousel)

### 규칙 (Rule)

자동으로 움직이거나 변경되는 콘텐츠는 사용자가 직접 제어할 수 있어야 한다. (정지 / 다음 / 이전 / 전체 보기 등)

### 문제 패턴 (Common Failure Patterns)

- **정지 버튼 부재**: 캐러셀 슬라이드가 자동 전환되며 정지 버튼 없음
- **조작 UI 부재**: 배너/이미지가 자동으로 움직이는데 조작 UI 부재
- **전환 속도 과다**: 움직임이 빠르고 사용자 인지 시간 부족
- **접근성 방해**: 스크린리더 사용자가 콘텐츠를 읽는 도중 슬라이드 변경

### 해결책 (Fix Guidelines)

- **제어 버튼 제공**: 콘텐츠 근처에 이전/다음/정지 버튼 제공
- **전환 간격**: 자동 슬라이드는 기본 5초 이상 간격
- **모션 감소 지원**: 모션 감소 설정(reduce motion) 시 자동재생 중지
- **대체 조작**: 스와이프/탭 등 대체 조작 제공
- **전체 보기**: 슬라이드 인덱스 표시 및 전체 보기 옵션 제공

### 필수 컨트롤

- **이전/다음**: 이전(prev/previous/이전/이전보기/이전글/이전배너), 다음(next/다음/다음보기/다음글/다음배너)
- **정지/재생**: 정지(pause/stop/정지/일시정지/멈춤), 재생(play/재생/멈추기)
- **전체 보기**: 전체보기(full/전체 보기/전체보기)
- **인덱스 표시**: 슬라이드 인덱스 (예: "1 / 4")

### 검사 기준

- 프레임 이름 또는 내부 텍스트에 자동 콘텐츠 키워드가 포함되거나 슬라이드 인덱스 패턴(예: "1 / 4")이 보이면 해당 프레임을 **정보 알림(info)**으로 표시
- 자동 제어(이전/다음/정지/전체보기) 존재 여부는 자동 판정하지 않음 → 디자이너/개발자가 직접 확인
- 짧은 탭/필터/날짜 선택 그룹(2~8개 버튼, 짧은 텍스트) 등은 자동 콘텐츠로 간주하지 않음
- 키워드가 포함돼도 `자동차` 등 콘텐츠 의미상 자동재생과 무관한 단어는 제외

---

## 검사 우선순위

1. **Error (오류)**: 텍스트 대비 3:1 미만
2. **Warning (경고)**: 
   - UI 요소/아이콘 대비 3:1 미만
   - 색상만으로 구분되는 인디케이터 그룹
   - 자동 콘텐츠 제어 컨트롤 부재

---

## 참고 자료

- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)
- 앱 접근성 가이드 (한국)

---

## 비검사 대상 (장식/이미지 제외 원칙)

- 이름에 `image/img/illust/illustration/decor/photo/picture/pic/thumbnail/thumb/avatar/logo` 등이 포함된 장식·이미지는 색상 의존성/명암비 검사를 건너뜀
- UI 키워드(버튼/탭/링크 등), 컴포넌트/인스턴스, 인터랙션(reactions) 등이 없는 경우 단순 그래픽으로 간주하여 검사 제외
- 주변 라벨 텍스트가 없고 작은 단일 아이콘·벡터(짧은 변 48px 이하)는 검사 제외
- 테이블/그리드/정적 배열(`table/grid/row/column/header` 등 이름)과 백플레이트/히트 영역(`bg/background/hit area`)은 검사 제외

---

## 플러그인 설정

Figma Accessibility Checker 플러그인에서 다음 설정을 조정할 수 있습니다:

- **검사 범위**: 선택 영역 / 현재 페이지 / 문서 전체
- **검사 규칙**: 각 규칙별 활성화/비활성화
- **대비 기준**: 명도 대비 임계값 조정
  - 텍스트 대비: 기본값 3:1 (최소 3:1 이상)
  - UI 요소 대비: 기본값 3:1 (최소 2:1 이상)

---

## 변경 이력

- 2025-12-02: 선택 영역 검사 시 선택된 노드/하위 노드만 색상 의존성 검사하도록 필터 추가
- 2025-12-02: 큰 텍스트(24px 이상 또는 18.67px Bold 이상) 대비 판정 로직 개선, 3:1 기준 정확히 적용
- 2025-11-28: 명도 대비 기준을 텍스트 포함 3:1로 통일, 4.5:1 규칙 제거
- 2025-11-28: 장식/히트 영역 배경 제외 규칙 추가 (명명/투명도/백플레이트 조건)
- 2025-11-28: 도형/아이콘 대비가 1:1(또는 3:1 미만)으로 계산되는 동일 요소는 검사 대상에서 제외하도록 명시
- 2025-11-28: 문서 초안 작성 (핵심 규칙 3종 정리)
